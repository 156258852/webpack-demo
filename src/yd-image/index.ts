import {
  screenshot,
  exitScreenshot,
} from './utils/screenshot';
import { initEdit } from './ImageEditor/EditorUtils';
import { copyCanvasImageToClipboard } from './utils/dataUtils';

export default {
  screenshot,
  exitScreenshot,
  initEdit,
  copyCanvasImageToClipboard,
};
