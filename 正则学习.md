奇奇怪怪的bug

```js
const arr = [1,2,'a', 'a', 'a', 'bb']
const reg = /(a)/g

arr.map((item,index)=>{
  if(reg.test(item)){
    console.log('reg.lastIndex',reg.lastIndex)
    console.log('index:',index, 'item:',item)
  }
})
预期是每一项是a的都匹配到，但是在使用全局正则表达式（带有 g或者y 标志）时，lastIndex 属性会在每次调用 test 或 exec 方法成功匹配后被更新为匹配结束后的下一个字符的位置。这意味着如果一个字符串中包含多个匹配项，lastIndex 会继续增加，指向下一个可能的匹配位置。


```

### 正则学习

```js
* 表示 [0,+无穷大)
+ 表示 [1,+无穷大)
*? 惰性匹配 懒惰匹配在第一次匹配时停止。
$& biao's
ha-ha,haa-haa
/(ha)-\1,(haa)-\2/g  //  1 表示分组的顺序
/(?:ha)-ha,(haa)-\1/g  // (?:)非捕获组，但我们用 \1 引用的第一个组实际上是指向第二个组，因为第一个是未被捕获的分组。
---------------------
 
const str = 'Date: 4 Aug 3PM'

正向先行断言: (?=)
=>  /\d+(?=PM)/g //表示匹配前面是数字的PM

负向先行断言: (?!)
=> /\d+(?!PM)/g //表示匹配数字后面不能根PM

-----------------------------
 const str = 'Product Code: 1064 Price: $5'
正向后行断言: (?<=)
=> /(?<=\$)\d+/g //为了只匹配前面带有 $ 的数字。
负向后行断言: (?<!)
=> /(?<!\$)\d+/g //为了只匹配前面没有 $ 的数字。
         

```

```
aliyunTags
```

